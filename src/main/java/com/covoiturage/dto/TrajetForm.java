package com.covoiturage.dto; import jakarta.validation.constraints.*;import org.springframework.format.annotation.DateTimeFormat; import lombok.Data;import java.time.LocalDateTime; @Data public class TrajetForm{@NotBlank private String depart;@NotBlank private String arrivee;@Min(1) private Integer dureeMinutes=30;@PositiveOrZero private Double prix=0.0;@Min(1) private Integer maxPlaces=1;@DateTimeFormat(pattern="yyyy-MM-dd'T'HH:mm") private LocalDateTime startDateTime;@DateTimeFormat(pattern="yyyy-MM-dd'T'HH:mm") private LocalDateTime endDateTime;private boolean recurring=false;private WeeklyScheduleDto weekly=new WeeklyScheduleDto();private String weeklySchedule="";}
